# cnpg-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: cnpg-service
spec:
  type: NodePort
  selector:
    cnpg.io/cluster: cnpg-cluster
    role: primary
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
